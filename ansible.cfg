[defaults]
inventory = inventory.yml
collections_paths = ~/.ansible/collections:/usr/share/ansible/collections
interpreter_python = auto_silent

[inventory]
enable_plugins = netbox.netbox.nb_inventory, yaml, ini, auto, constructed
